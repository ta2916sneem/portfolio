{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Raiyan portfolio app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ts-node portfolio-blog-backend/app.ts",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix portfolio-blog-backend && npm install --prefix portfolio-react && npm run build --prefix portfolio-react && npm install -g ts-node"
  },
  "engines": {
    "node": "15.x"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.6"
  },
  "devDependencies": {
    "@types/node": "^14.14.12"
  }
}
